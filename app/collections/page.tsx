import type { Metadata } from 'next';
import { ArrowUpRight } from 'lucide-react';
import { PageIntro,AppointmentBanner,Eyebrow } from '@/components/bridal/shared';
import { collections } from '@/lib/bridal';
export const metadata:Metadata={title:'Designer Wedding Dress Collections',description:'Explore Nicole Milano, Luna Novias, Adriana Alier and White One at Courtyard Bridal in Tollesbury, Essex. View the collections and guide prices.',alternates:{canonical:'/collections'}};
export default function Collections(){return <main id="main-content"><PageIntro eyebrow="The designer edit" title="There’s a dress" italic="with your name on it." text="Some brides know exactly what they’re looking for. Others find it in the dress they never expected to try. Start with a little inspiration."/><nav className="collection-jump-links" aria-label="Jump to a designer">{collections.map(c=><a href={'#'+c.slug} key={c.slug}>{c.name}</a>)}</nav><section className="section compact-section">{collections.map((c,i)=><article className="designer-detail" id={c.slug} key={c.slug}><div className="designer-detail-photo"><img src={'/images/'+c.image+'.webp'} alt={c.name+' bridal gown'} width="900" height="1200" loading={i===0?'eager':'lazy'}/></div><div className="designer-detail-copy"><Eyebrow>0{i+1} / {c.caption}</Eyebrow><h2>{c.name}</h2><p>{c.description}</p><p>{c.note}</p><p className="designer-price">{c.price}<small>COLLECTION PRICE GUIDE · AVAILABILITY ON ENQUIRY</small></p><a href={'/appointments?designer='+encodeURIComponent(c.name)} className="button">Ask about this collection <ArrowUpRight size={18}/></a><div><a href={'/gallery?designer='+encodeURIComponent(c.name)} className="text-link">See the dress edit <ArrowUpRight size={16}/></a></div></div></article>)}<p className="pricing-note">Prices are taken from our published collection guide and may change. Please check current prices, sample sizes and availability with us before your visit. Photographs show designer styles and do not guarantee a particular dress is in stock.</p></section><AppointmentBanner/></main>}

